<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="BaseIndustrialButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground}"/>
        <Setter Property="Background" Value="{StaticResource Brush.Background.Subtle}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border.Strong}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
        Background="{Binding Background, RelativeSource={RelativeSource TemplatedParent}, FallbackValue={StaticResource Brush.Background.Subtle}, TargetNullValue={StaticResource Brush.Background.Subtle}}"
        BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource TemplatedParent}, FallbackValue={StaticResource Brush.Border.Strong}, TargetNullValue={StaticResource Brush.Border.Strong}}"
        BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource Brush.Status.Blue}"/>
                            <Setter Property="Background" Value="#FFF0F7FF"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#FFE5E5E5"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Brush.Background.Disabled}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
                            <Setter Property="Foreground" Value="#FF888888"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="NavButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseIndustrialButtonStyle}">
        <Setter Property="Height" Value="45"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>
    <Style x:Key="ControlButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseIndustrialButtonStyle}">
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>
    <Style x:Key="BlueButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseIndustrialButtonStyle}">
        <Setter Property="Background" Value="{StaticResource Brush.Status.Blue}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground.Inverse}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Status.Blue}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FF3B8EDB"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#FF125EA9"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="ResetButtonStyle" TargetType="Button" BasedOn="{StaticResource BlueButtonStyle}">
        <Setter Property="Height" Value="22"/>
        <Setter Property="Padding" Value="5,0"/>
        <Setter Property="FontSize" Value="12"/>
    </Style>
    <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseIndustrialButtonStyle}">
        <Setter Property="Background" Value="{StaticResource Brush.Status.Blue}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground.Inverse}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Status.Blue}"/>
    </Style>
    <Style x:Key="SuccessButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseIndustrialButtonStyle}">
        <Setter Property="Background" Value="{StaticResource Brush.Status.Green}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground.Inverse}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Status.Green}"/>
    </Style>
    <Style x:Key="WarningButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseIndustrialButtonStyle}">
        <Setter Property="Background" Value="{StaticResource Brush.Status.Yellow}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground.Inverse}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Status.Yellow}"/>
    </Style>
    <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseIndustrialButtonStyle}">
        <Setter Property="Background" Value="{StaticResource Brush.Status.Red}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground.Inverse}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Status.Red}"/>
    </Style>
    <Style x:Key="IndustrialPanelBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Brush.Background}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="Margin" Value="5"/>
    </Style>
    <Style x:Key="IndustrialTextBlockStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground}"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>
    <Style x:Key="IndustrialTitleTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource IndustrialTextBlockStyle}">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
    <Style x:Key="IndustrialInputStyle" TargetType="TextBox">
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground}"/>
        <Setter Property="Background" Value="{StaticResource Brush.Background}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border.Strong}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Margin" Value="5"/>
    </Style>
    <Style x:Key="IndustrialComboBoxStyle" TargetType="ComboBox">
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground}"/>
        <Setter Property="Background" Value="{StaticResource Brush.Background}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border.Strong}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Margin" Value="5"/>
    </Style>
    <Style TargetType="Button" BasedOn="{StaticResource BaseIndustrialButtonStyle}"/>
    <Style TargetType="TextBox" BasedOn="{StaticResource IndustrialInputStyle}"/>
    <Style TargetType="ComboBox" BasedOn="{StaticResource IndustrialComboBoxStyle}"/>
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style TargetType="GroupBox">
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>
    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{StaticResource Brush.Background}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground}"/>
    </Style>
    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource Brush.Background}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="RowBackground" Value="{StaticResource Brush.Background}"/>
        <Setter Property="AlternatingRowBackground" Value="{StaticResource Brush.Background.Subtle}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground}"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
    </Style>
</ResourceDictionary>
