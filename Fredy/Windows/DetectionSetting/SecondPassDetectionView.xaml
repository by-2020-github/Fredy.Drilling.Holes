<Window x:Class="Fredy.Drilling.Holes.Views.SecondPassDetectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Fredy.Drilling.Holes.ViewModels"
             xmlns:models="clr-namespace:Fredy.Drilling.Holes.Models"
             mc:Ignorable="d" 
             Title="二道圈探测设置"
             Background="{StaticResource Brush.Background.Subtle}"
             d:DesignHeight="600" d:DesignWidth="450"    
             d:DataContext="{d:DesignInstance Type=vm:SecondPassDetectionViewModel, IsDesignTimeCreatable=True}">

    <Window.DataContext>
        <vm:SecondPassDetectionViewModel/>
    </Window.DataContext>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="0 代表本圈不探测，其他数字 N 代表本圈探测次数" 
                   Foreground="{StaticResource Brush.Status.Green}" HorizontalAlignment="Center" Margin="0,0,0,15" FontSize="14"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10">
            <ItemsControl ItemsSource="{Binding DetectionItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:SecondPassDetectionItem}">
                        <Grid Margin="45,2" Height="30">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding Index, StringFormat='第{0}圈'}" VerticalAlignment="Center" FontSize="14"/>

                            <TextBox Grid.Column="1" Text="{Binding DetectionCount}" 
                                     VerticalContentAlignment="Center" HorizontalContentAlignment="Left" Padding="5,0"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,20">
            <TextBlock Text="探测偏移量阈值：" VerticalAlignment="Center" FontSize="14" FontWeight="Bold"/>
            <TextBox Text="{Binding OffsetThreshold}" Width="150" Height="28" VerticalContentAlignment="Center" Padding="5,0"/>
        </StackPanel>

        <UniformGrid Grid.Row="3" Columns="2" Margin="40,0">
            <Button Content="确定" Command="{Binding ConfirmCommand}" Height="35" Margin="10,0" Style="{StaticResource BlueButtonStyle}"/>
            <Button Content="重置" Command="{Binding ResetCommand}" Height="35" Margin="10,0" Style="{StaticResource BlueButtonStyle}"/>
        </UniformGrid>
    </Grid>
</Window>
