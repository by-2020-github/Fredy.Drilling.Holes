<Window x:Class="Fredy.Drilling.Holes.Views.DetectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Fredy.Drilling.Holes.ViewModels"
             xmlns:models="clr-namespace:Fredy.Drilling.Holes.Models"
             mc:Ignorable="d" 
             Background="#C0C0C0"
             Title="一道圈探测设置"
             d:DesignHeight="600" d:DesignWidth="450"
             d:DataContext="{d:DesignInstance Type=vm:DetectionViewModel, IsDesignTimeCreatable=True}">

    <Window.DataContext>
        <vm:DetectionViewModel/>
    </Window.DataContext>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Foreground="Green" Margin="0,0,0,15" HorizontalAlignment="Center" FontSize="14">
            0 代表本圈不探测，其他数字 N 代表本圈探测次数
        </TextBlock>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,15">
            <ItemsControl ItemsSource="{Binding RingItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:DetectionRingItem}">
                        <Grid Margin="40,3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding Index, StringFormat='第{0}圈'}" VerticalAlignment="Center" FontSize="14"/>

                            <TextBox Grid.Column="1" Text="{Binding DetectionCount}" Height="28" VerticalContentAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="探测偏移量阈值：" VerticalAlignment="Center" FontSize="14" FontWeight="Bold"/>
            <TextBox Text="{Binding OffsetThreshold}" Width="150" Height="28" VerticalContentAlignment="Center"/>
        </StackPanel>

        <UniformGrid Grid.Row="3" Columns="2" Margin="40,0">
            <Button Content="确定" Command="{Binding ConfirmCommand}" Height="35" Margin="10,0" Style="{StaticResource BlueButtonStyle}"/>
            <Button Content="重置" Command="{Binding ResetCommand}" Height="35" Margin="10,0" Style="{StaticResource BlueButtonStyle}"/>
        </UniformGrid>
    </Grid>
</Window>