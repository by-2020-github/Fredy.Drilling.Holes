<Window x:Class="Fredy.Drilling.Holes.Views.PunchingCompensationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Fredy.Drilling.Holes.ViewModels" xmlns:models="clr-namespace:Fredy.Drilling.Holes.Models"
             mc:Ignorable="d"
             Background="{StaticResource Brush.Background.Subtle}"
        Title="冲孔补偿设置（单位:脉冲->mm更优）"
             d:DesignHeight="600" d:DesignWidth="500"
             d:DataContext="{d:DesignInstance Type=vm:PunchingCompensationViewModel, IsDesignTimeCreatable=True}">

    <Window.DataContext>
        <vm:PunchingCompensationViewModel />
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="冲孔补偿设置" FontWeight="Bold" FontSize="14" />
            <TextBlock Text=" (单位：脉冲)" Margin="5,0,0,0" VerticalAlignment="Bottom" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="80,0,20,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="头道补偿" VerticalAlignment="Center" Margin="0,0,5,0" />
                <Button Content="重置" Command="{Binding ResetFirstPassCommand}" Cursor="Hand" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="二道补偿" VerticalAlignment="Center" Margin="0,0,5,0" />
                <Button Content="重置" Command="{Binding ResetSecondPassCommand}" Cursor="Hand" />
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="二道表面降低" VerticalAlignment="Center" Margin="0,0,5,0" />
                <Button Content="重置" Command="{Binding ResetSurfaceReductionCommand}" Cursor="Hand" />
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding CompensationItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,2" d:DataContext="{d:DesignInstance Type= models:PunchingCompensationItem}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding Index, StringFormat='第{0}圈'}" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                            <TextBox Grid.Column="1" Text="{Binding FirstPassComp}" Margin="5,2" Height="25"/>
                            <TextBox Grid.Column="2" Text="{Binding SecondPassComp}" Margin="5,2" Height="25"/>
                            <TextBox Grid.Column="3" Text="{Binding SurfaceReduction}" Margin="5,2" Height="25"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="3" Content="确定" Command="{Binding ConfirmCommand}" 
                Width="100" Height="30" Margin="0,10,0,0" HorizontalAlignment="Center"/>
    </Grid>
</Window>
